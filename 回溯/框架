# 框架
result = []

def backtrack(path,choose_list):

    if 满足结束条件：
        result.append(path)
        return
    for choice in choose_list:
        做选择
        backtrack(path,choose_list)
        撤销选择
    
